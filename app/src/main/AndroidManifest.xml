<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.myandroid">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/rem"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CalculatorActivity"></activity>
        <activity android:name=".PictureActivity" />
        <activity android:name=".IconActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".TransformActivity" />
        <activity android:name=".ScreenShotActivity" />
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StretchActivity" />
        <activity android:name=".ButtonActivity" />
        <activity android:name=".ChatLiveActivity" />
        <activity android:name=".PaomdActivity" />
        <activity android:name=".ScrollActivity" />
        <activity android:name=".GravityActivity" />
        <activity android:name=".MarginPaddingActivity" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>